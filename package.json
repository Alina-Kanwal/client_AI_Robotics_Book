{
  "name": "physical-ai-book-root",
  "version": "1.0.0",
  "scripts": {
    "build": "bash build.sh",
    "dev": "cd website && npm install && npm run start"
  },
  "engines": {
    "node": ">=18.0"
  }
}